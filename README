MyHadoop is a simple web frontend for hadoop.

Functions
---------
1. Upload source code
2. Execute bash script
3. Download results

Configuration
-------------
1. Edit /MyHadoop/myhadoop/scripts/hadoop.sh
Let it generate the result file in /MyHadoop/myhadoop/static/output/
2. Edit execute() in /MyHadoop/myhadoop/utils/hadoopfile.py
Let it return the filename of the result file (don't include path)
